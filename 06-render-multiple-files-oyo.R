library(quarto)
library(tidyverse)

state_inputs <- c(state.name)   # state.name gives a vector of strings all 50 states

outage_reports <- tibble(
  input = 
  output_file = 
  execute_params = 
)

pwalk ()