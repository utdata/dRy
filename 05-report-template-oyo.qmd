---
title: "04-report-template-oyo"
params:
  state: "Texas"
echo: true
---

```{r}
#| label: setup
#| mesage: false
#| warning: false

library(tidyverse)
library(janitor)
```

```{r}
#| label: import

#copy from analysis
power_cleaned_list <- list.files(
  "data-processed-power",
  full.names = T
)

power_cleaned_list
```

```{r}
#| label: combine

#copy from analysis
all_outages <- map(
  power_cleaned_list,
  read_csv,
  col_type = cols(fips = col_character())
) |>
  list_rbind() |>
  clean_names()

all_outages
```

## OYO: create parameter list and filter for parameters



```{r}
#state_list <- 
```

States: (check your work)

```{r}
#outages <- all_outages |>

#outages
```